Once the files are installed open up vscode and open up the terminal
run the following commands:
npm install express
npm install ejs
npm install mysql

to run the index.js type the command:
node index.js
